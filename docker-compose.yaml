version: '3.9'

services:
  # streamplay.api:
  #   image: ${DOCKER_REGISTRY-}streamplayapi
  #   container_name: streamplay.Api
  #   build:
  #     context: .
  #     dockerfile: src/API/streamplay.Api/Dockerfile
  #   ports:
  #     - 5000:8080
  #     - 5001:8081

  # streamplay.database:
  #   image: postgres:latest
  #   container_name: streamplay.Database
  #   environment:
  #     - POSTGRES_DB=streamplay
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #   volumes:
  #     - ./.containers/db:/var/lib/postgresql/data
  #   ports:
  #     - 5432:5432

  streamplay.identity:
    image: quay.io/keycloak/keycloak:latest
    container_name: streamplay.Identity
    command: start-dev --import-realm
    environment:
      - KC_HEALTH_ENABLED=true
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
    volumes:
      - ./.containers/identity:/opt/keycloak/data
      - ./.files:/opt/keycloak/data/import
    ports:
      - 18080:8080

  streamplay.seq:
    image: datalust/seq:latest
    container_name: streamplay.Seq
    environment:
      - ACCEPT_EULA=Y
    ports:
      - 5341:5341
      - 8081:80

  # streamplay.redis:
  #   image: redis:latest
  #   container_name: streamplay.Redis
  #   restart: always
  #   ports:
  #     - 6379:6379

  # streamplay.jaeger:
  #   image: jaegertracing/all-in-one:latest
  #   container_name: streamplay.Jaeger
  #   ports:
  #     - 4317:4317
  #     - 4318:4318
  #     - 16686:16686
